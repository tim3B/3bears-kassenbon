# --- Netlify Function (server-side only — NEVER commit real values) ---
ANTHROPIC_API_KEY=sk-ant-...              # the ROTATED Claude key
ANTHROPIC_MODEL=claude-opus-4-8            # most accurate OCR; or claude-sonnet-5 for lower cost
SUPABASE_URL=https://<ref>.supabase.co
SUPABASE_SERVICE_ROLE=eyJ...              # service_role key (server-only)

# --- Frontend (public — safe to expose; injected into public/config.js) ---
# SUPABASE_URL (same as above)
# SUPABASE_ANON_KEY=eyJ...                # anon/public key
